<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <!-- Iconos -->
    <script src="https://kit.fontawesome.com/e41faadd8e.js" crossorigin="anonymous"></script>
    <!--  Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300&display=swap" rel="stylesheet">
    <title>Editar perfil</title>
</head>
<body class="user">
    <%- include('../partials/header') %>
    <main>
        <form action="/users/editarUser/<%= locals.userLogin.id %>?_method=PUT" method="post" enctype="multipart/form-data"> 
            
          <section class="img"> 
            <img class="avatar" src="../img/usuarios/<%= locals.userLogin.image %>" alt=""> 
          </section>
            <div> 
                <label> foto de perfil</label>
                <input type="file" name="image" id="file" accept="/image" value="<%= locals.old && old.image ? old.name: locals.userLogin.image %>" >
            </div>
                  <ul>
                    <li>Editar nombre
                      <input type="text" name="name" id="name" value="<%=locals.old && old.name ? old.name : locals.userLogin.nombre %>">
               </li>
               <li>Editar apellido
                <input type="text" name="apellido" id="apellido" value="<%=locals.old && old.apellido ? old.apellido : locals.userLogin.apellido %>">
         </li>
         <!-- recordatorio para crear validacion -->
              <div>     
                    <li>Genero
                      <input type="genero" id="genero" name="genero" value="<%= locals.userLogin.genero%> ">
                    </li>
              </div>
                     
                    <div>
                        <button class="actualizar" type="submit">Actualizar datos</button>
                    </div>

</main>
    
    <%- include('../partials/footer') %>
    
        </form>

      </main>